// RUN: run
// EXPECT: stdout "Nested"
fn outer(x: i32) -> i32 {
    fn inner(y: i32) -> i32 {
        return y * 2;
    }
    return inner(x) + 10;
}
fn main() -> i32 {
    println("Nested");
    return outer(5);
}
