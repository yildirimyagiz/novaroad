// RUN: run
// EXPECT: stdout "Borrow check passed"
// EXPECT-EXIT: 0

fn main() -> i32 {
    let x: i32 = 42;
    let y: i32 = x;  // copy for i32
    let z: i32 = x;  // another copy, should work
    println("Borrow check passed");
    return 0;
}
