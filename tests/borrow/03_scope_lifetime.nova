// RUN: run
// EXPECT: stdout "Lifetime OK"
fn main() -> i32 {
    let x: i32 = 10;
    {
        let y: i32 = x;
    }
    let z: i32 = x;  // OK, y out of scope
    println("Lifetime OK");
    return z;
}
