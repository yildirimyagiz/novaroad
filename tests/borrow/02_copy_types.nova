// RUN: run
// EXPECT: stdout "Copy types OK"
fn main() -> i32 {
    let a: i32 = 10;
    let b: i32 = a;
    let c: i32 = a;  // OK for copy types
    println("Copy types OK");
    return a + b + c;
}
