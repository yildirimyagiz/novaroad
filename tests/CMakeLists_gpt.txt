# GPT Test Suite CMakeLists.txt
# Add this to tests/CMakeLists.txt or include it

# GPT Inference Test
add_executable(test_gpt_inference
    test_gpt_inference.c
    ${CMAKE_SOURCE_DIR}/src/ai/gpt/nova_gpt_backend.c
    ${CMAKE_SOURCE_DIR}/src/ai/nn/flash_attention_v2.c
    ${CMAKE_SOURCE_DIR}/src/compiler/backend/cpu/flash_attention_cpu.c
)

target_include_directories(test_gpt_inference PRIVATE
    ${CMAKE_SOURCE_DIR}/include
    ${CMAKE_SOURCE_DIR}/src/compiler/backend
)

target_link_libraries(test_gpt_inference PRIVATE
    nova_ai
    nova_std
    m
)

# Add test to CTest
add_test(NAME GPT_Inference COMMAND test_gpt_inference)

# Set test properties
set_tests_properties(GPT_Inference PROPERTIES
    TIMEOUT 30
    LABELS "gpt;inference;integration"
)

message(STATUS "✅ GPT test suite configured")
