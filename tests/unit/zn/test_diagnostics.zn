// Diagnostics + Span Test
// Test error reporting with source locations
// XFAIL: Intentional type error for testing diagnostics

fn test_with_error() {
    // This should generate a diagnostic error
    var invalid: InvalidType = 42; // InvalidType doesn't exist
    yield invalid;
}

fn test_diagnostics() {
    // Test diagnostics collection (runtime implementation needed)
    yield 42;
}

fn main() {
    var result: usize = test_diagnostics();
    yield result;
}
