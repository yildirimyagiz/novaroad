// Array Types Test
// Tests static array types

fn test_arrays() {
    // Static array type annotation
    var arr: [i32; 4];

    // Array indexing (not implemented yet, just declare)
    // arr[0] = 10;

    yield 42; // Placeholder
}

fn main() {
    var result: i32 = test_arrays();
    yield result;
}
