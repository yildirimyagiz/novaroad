// 🌐 Nova Standard UI Library — v10.0
// This is the common entry point for Sovereign UI across platforms.

use mobile::ui as mobile_ui;
use mobile::ui::*;

/// 🚀 Mounts a Sovereign Component to the primary application surface.
/// Works across Native (Metal), Web (WebGPU/Canvas), and Mobile (Aurora).
open fn mount(component: any) {
    println("🚀 [Sovereign] Mounting component state to hardware-accelerated viewport...");
    // In a real system, this would trigger the initial render and setup reactive listeners.
    // For Stage 0 bootstrap, we just acknowledge the mount request.
}

/// 🎨 Global Styling Tools
open data AppTheme {
    open primary_color: Color,
    open dark_mode: bool,
}

open fn apply_theme(theme: AppTheme) {
    println("🎨 Applying Sovereign theme...");
}
