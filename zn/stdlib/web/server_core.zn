// Nova Web: Server Core

expose data ServerConfig {
    host: String,
    port: i32,
    workers: i32,
}

expose data Response {
    status: i32,
    body: String,
    headers: Vec<(String, String)>,
}

expose fn start(config: ServerConfig) -> bool {
    yield true;
}
