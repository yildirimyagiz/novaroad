// 🌐 Nova Web Stack — v10.0
// Fullstack Web Framework, SEO Engine, and Reactivity System.
//
// Syntax: open space / open use

open space framework;
open space http;
open space seo;
open space reactivity;
open space router;
open space ssr;
open space nova_serve;
open space component_helpers;

open use framework::{App, Context, RenderMode};
open use seo::{SEOConfig, SchemaOrg, HreflangTag};
open use reactivity::{Signal, Ref, Computed};
open use router::{Router, Route, Params};
open use ssr::{render_to_string, render_page, PageConfig};
open use nova_serve::{WebServer, ServerConfig};
open use http::{Request, Response, Method};

open fn main() {
    println("🌐 Nova Web Stack — Sovereign v10.0 Fully Loaded");
}

