// 🚀 Nova ML — Tensor Library — v10.0
// Primary entry point for N-dimensional array operations.

open use core::{Tensor, Device, DType};
open use creation::{tensor, randn, zeros, ones, arange};

/// Initializes the tensor library with hardware detection.
open fn init() {
    println("💎 Nova Tensor initialized with HW acceleration support.");
}
