// ⚙️ Nova ML — Optimizers — v10.0
// Native implementation of high-performance training optimizers.

open use optimizer::Optimizer;
open use sgd::SGD;
open use adam::{Adam, AdamW};
// RMSprop, Adagrad, etc. are available via their modules.

/// Initializer for the optimizer suite.
open fn init() {
    println("⚙️ Nova Optimizers initialized (SGD, Adam, AdamW, etc.)");
}
