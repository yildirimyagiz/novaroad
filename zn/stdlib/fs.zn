// 📂 Nova Standard Library — File Operations
// Simplified for Stage 0 bootstrap

expose fn read_to_string(path: String) -> String {
    // This will be linked to the runtime implementation
    yield "";
}

expose fn write(path: String, contents: String) {
    // This will be linked to the runtime implementation
}
