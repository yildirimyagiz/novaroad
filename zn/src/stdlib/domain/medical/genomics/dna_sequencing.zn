@module("medical::genomics::dna_sequencing")
@export
open data GenomeSequence {
    reads: Vec<String>,
    coverage: f64
}
impl GenomeSequence {
    fn quality_score(&self) -> f64 {
        self.coverage / 30.0
    }
}
