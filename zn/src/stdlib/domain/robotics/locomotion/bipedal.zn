@module("robotics::locomotion::bipedal")
@export
open data BipedalRobot {
    leg_length: f64,
    step_frequency: f64
}
impl BipedalRobot {
    fn walking_speed(&self) -> f64 {
        self.leg_length * self.step_frequency
    }
}
