// JWT — sign, verify, claims

open data Claims {
    sub: String,
    exp: i64,
    iat: i64,
}

open data JWT;

impl JWT {
    fn sign(claims: Claims, secret: String) -> String {
        // base64(header).base64(payload).signature
        "".into()
    }
    fn verify(token: String, secret: String) -> Option<Claims> {
        None
    }
}
