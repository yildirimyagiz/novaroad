// Session & session store — HTTP auth

open data Session {
    id: String,
    user_id: String,
    expires_at: i64,
}

open rules SessionStore {
    fn get(session_id: String) -> Option<Session>;
    fn set(session: Session);
    fn delete(session_id: String);
}
