// ╔═══════════════════════════════════════════════════════════════════════════════╗
// ║           NOVA STANDARD LIBRARY - WEB MODULE                                  ║
// ║      Fullstack Web Framework, SEO Engine, and Reactivity System               ║
// ╚═══════════════════════════════════════════════════════════════════════════════╝

use .framework;
use .seo;
use .reactivity;
use .router;
use .ssr;
use .nova_serve;

/// Framework exports
expose use .framework::{App, Context, RenderMode};

/// SEO exports
expose use .seo::{SEOConfig, SchemaOrg, HreflangTag};

/// Reactivity exports
expose use .reactivity::{Signal, Ref, Computed};

/// Router exports
expose use .router::{Router, Route, Params};

/// SSR exports
expose use .ssr::{render_to_string, render_page, PageConfig};

/// Server exports
expose use .nova_serve::{WebServer, ServerConfig};

open fn main() {
    println("🌐 Nova Web Stack — Sovereign v8.0 Fully Loaded");
}
