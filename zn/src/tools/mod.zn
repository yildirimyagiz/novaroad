// ╔═══════════════════════════════════════════════════════════════════════════════╗
// ║  Nova Tools Module - Main Module v1.0                                            ║
// ║  Central hub for all Nova development tools                                      ║
// ╚═══════════════════════════════════════════════════════════════════════════════╗

// Export all tool modules
pub mod infra;
pub mod ai;
pub mod formal;
pub mod advanced;
pub mod toolchain;

// Community tools
pub mod community_tools;

// Re-exports for convenience

// Infrastructure tools (compilation speedup)
pub use infra::{
    incremental_compilation, incremental, watch_mode, parallel_compilation,
    cache_server, jit_compilation, precompiled_binaries, distributed_compilation,
    cloud_compilation, advanced_pgo, example_usage,
    IncrementalBuildManager, FileWatcher, ParallelCompiler, 
    DistributedCoordinator, CloudCompilationService, AdvancedPGOOptimizer
};

// AI tools (SWE-1.5 integration)
pub use ai::{
    SWE_15_Nova_System, SWE_15_Bridge, AIModel, AutonomousAgent,
    BugFixVerificationPipeline, RepositoryAnalyzer, MockSWE_15_Bridge, default_mock_config
};

// Formal verification tools
pub use formal::{
    GodelCache, NeuralOptimizer, FormalProver,
    AdaContractManager, AdaTypeManager, AdaVerificationManager,
    VerifiedUnsafeRegistry, VerifiedUnsafeOp, VerificationStatus
};

// Advanced tools
pub use advanced::{
    AdaSafetyManager, SafetyConstraint, SafetyLevel, register_default_constraints
};

// Toolchain tools
pub use toolchain::{
    zndoc_main, znfmt_main, znlint_main, znpkg_main, znrepl_main, zntest_main, znup_main
};
