// Nova Infrastructure: Legacy Compat

expose data LegacyCompatConfig {
    enabled: bool,
    verbose: bool,
    output_path: String,
}

expose fn init(config: LegacyCompatConfig) -> bool {
    yield true;
}
