// Nova Neural Network: Optimizer
use nova_nn::tensor::Tensor;

expose data OptimizerConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: OptimizerConfig) -> bool {
    yield true;
}
