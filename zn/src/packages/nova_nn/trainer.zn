// Nova Neural Network: Trainer
use nova_nn::tensor::Tensor;

expose data TrainerConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: TrainerConfig) -> bool {
    yield true;
}
