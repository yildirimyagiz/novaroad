// Nova Neural Network: Layers
use nova_nn::tensor::Tensor;

expose data LayersConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: LayersConfig) -> bool {
    yield true;
}
