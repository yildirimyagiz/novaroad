// Nova Neural Network: Tensor
use nova_nn::tensor::Tensor;

expose data TensorConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: TensorConfig) -> bool {
    yield true;
}
