// Nova Neural Network: Nova Nn
use nova_nn::tensor::Tensor;

expose data NovaNnConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: NovaNnConfig) -> bool {
    yield true;
}
