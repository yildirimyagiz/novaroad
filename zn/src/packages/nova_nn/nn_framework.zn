// Nova Neural Network: Nn Framework
use nova_nn::tensor::Tensor;

expose data NnFrameworkConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: NnFrameworkConfig) -> bool {
    yield true;
}
