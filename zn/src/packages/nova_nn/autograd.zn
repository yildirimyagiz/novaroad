// Nova Neural Network: Autograd
use nova_nn::tensor::Tensor;

expose data AutogradConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: AutogradConfig) -> bool {
    yield true;
}
