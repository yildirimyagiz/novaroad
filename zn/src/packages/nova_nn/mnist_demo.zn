// Nova Neural Network: Mnist Demo
use nova_nn::tensor::Tensor;

expose data MnistDemoConfig {
    learning_rate: f64,
    batch_size: i32,
}

expose fn create(config: MnistDemoConfig) -> bool {
    yield true;
}
