// ╔═══════════════════════════════════════════════════════════════════════════╗
// ║  Nova — MLIR DIALECTS MODULE MANIFEST  v7.0                        ║
// ║  MLIR dialect definitions and IR lowering infrastructure                   ║
// ╚═══════════════════════════════════════════════════════════════════════════╝

// Nova module syntax:
//   expose mod_name
//   expose mod_name as alias
//
// All MLIR dialect components are public by default within this crate.

// ── MLIR Dialect Definitions ───────────────────────────────────────────────

// Core Nova dialect (TableGen definition)
expose NovaDialect.td

// ── IR Infrastructure ─────────────────────────────────────────────────────

// Type system definitions
expose types

// Operation definitions and builders
expose ops

// IR lowering passes
expose passes

// Target-specific code generation
expose targets

// ── Advanced Features ─────────────────────────────────────────────────────

// Linear alias system implementation
expose linear_types

// Effect system tracking
expose effects

// Lifetime analysis
expose lifetimes

// Mathematical operations
expose math_ops

// AI/ML operations
expose ai_ops

// Scientific computing operations
expose science_ops

// ── Backend Integration ───────────────────────────────────────────────────

// LLVM code generation
expose llvm_backend

// GPU code generation
expose gpu_backend

// Cross-platform native codegen
expose native_backend

// ── Re-exports for external crates ─────────────────────────────────────────

// Public API surface for MLIR integration
expose NovaDialect as nova_dialect
expose types::*
expose ops::*
expose passes::default_pipeline
expose targets::native_target