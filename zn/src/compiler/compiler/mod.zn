/**
 * ═══════════════════════════════════════════════════════════════════════════
 * NOVA COMPILER — ROOT MODULE — v10.0
 * ═══════════════════════════════════════════════════════════════════════════
 * Main entry point for the Nova compiler as a library.
 * This file defines the module hierarchy for the reorganized structure.
 */

// frontend: merged into crate::frontend (span, errors, parser, hir, ir, mir)
open use crate::frontend;

open space optimization {
    open space egraph;
    open space pattern_optimizer;
    open space learning_optimizer;
    open space adaptive_simd;
    open space superoptimizer;
}

open space analysis {
    open space smt;
    open space measurement;
}

open space codegen {
    open space codegen;
}

open space runtime {
    open space session;
    open space driver;
    open space jit;
}

open space utils {
    open space data_structures;
    open space interface;
    open space unified;
    open space integration;
}

// ── Re-exports for convenience ──────────────────────────────────────────────
open use runtime::driver::CompilationDriver;
open use runtime::session::{Session, CompileOptions};
open use crate::frontend::parser::Parser;
