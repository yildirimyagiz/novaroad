// Nova Compiler: Dead Code Elimination

expose data DeadCodeEliminationContext {
    source: String,
    options: Vec<String>,
}

expose fn process(ctx: DeadCodeEliminationContext) -> bool {
    yield true;
}
