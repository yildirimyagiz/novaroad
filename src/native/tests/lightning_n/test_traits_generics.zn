rules Logger {
  fn log(msg: str);
}

data ConsoleLogger {
  prefix: str
}

impl Logger for ConsoleLogger {
  fn log(msg: str) {
  }
}

fn generic_log<T>(logger: T, msg: str) {
}

fn main() {
  let logger = ConsoleLogger { prefix: "[LOG] " };
  generic_log<ConsoleLogger>(logger, "Hello Nova!");
}
