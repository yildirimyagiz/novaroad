/**
 * Stage 3 Parser — Reuses Stage 1 parser implementation.
 * Stage 3 focuses on optimization passes; the parser is unchanged.
 */

/* Stage 1 parser is linked via libstage1.a — no additional code needed here. */
