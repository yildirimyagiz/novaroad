/**
 * Stage 2 Parser — Reuses Stage 1 parser implementation.
 * Stage 2 adds semantic analysis and type system on top of Stage 1 frontend.
 * The parser is identical; type-aware parsing is handled by semantic layer.
 */

/* Stage 1 parser is linked via libstage1.a — no additional code needed here. */
